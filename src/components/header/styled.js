import styled from 'styled-components'

const StyledHeader = styled.header`
  
  width:100vw;
  transtion: 0.2s;
  svg {
    margin: 0.625rem;
    color: #ff6647;
  }

  svg:hover {
    width: 44px;
    height: 44px
  }
  svg:active {
    width: 48px;
    height: 48px
  }
  
`

export default StyledHeader